# Docker containers that host and interact with SQL databases

## SQL server container
A container that runs an Microsoft SQL Server database.

You can build and run it separately using the shell scripts inside the directory **sqlserver**.
Or it is started from the **docker-compose.yml** file in the main directory.

## Ubuntu container
A generic container running Ubuntu and a Python environment that can interact with the database container(s).